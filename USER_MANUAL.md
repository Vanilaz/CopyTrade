# 📘 คู่มือการติดตั้งและใช้งาน CopyTrade Pro v3.0.6 (ฉบับมือใหม่จับมือทำ)

คู่มือฉบับนี้จัดทำขึ้นเพื่อให้ **มือใหม่ (Beginner)** สามารถทำความเข้าใจและติดตั้งระบบ CopyTrade ข้ามพอร์ต/ข้ามเซิร์ฟเวอร์ได้ด้วยตัวเอง

---

## 🧭 ความเข้าใจพื้นฐานก่อนเริ่ม
ระบบนี้ทำงานโดยมีศูนย์กลาง 3 ส่วน:
1. **Master (แม่):** บัญชีที่จะทำการเทรดจริง
2. **Slave (ลูก):** บัญชีที่จะคอยนั่งรอลอกออเดอร์
3. **Relay Server (ศูนย์กระจายสัญญาณ):** ตัวทำหน้าที่เป็นสะพานเชื่อมให้แม่กับลูกคุยกันได้ (จำเป็นต้องมี!)

---

## 📌 บทที่ 1: การติดตั้งไฟล์ลงในโปรแกรม MT5 (ต้องทำทั้งเครื่องแม่และเครื่องลูก)

1. เปิดโปรแกรม **MetaTrader 5 (MT5)**
2. คลิกเมนูซ้ายบน **File** -> **Open Data Folder** (จะมีหน้าต่างแฟ้มเด้งขึ้นมา)
3. ทิ้งหน้าจอ MT5 ไว้ก่อน แล้วไปก๊อปปี้ไฟล์ **`CopyTradeMaster.ex5`** และ **`CopyTradeSlave.ex5`** ออกมา
4. นำไปวางในแฟ้มชื่อ `MQL5` -> `Experts` ยัดไว้ตรงนี้ได้เลย
5. ถัดมาไปเอาก๊อปปี้ไฟล์ **`SymbolMap.txt`** 
6. นำไปวางในแฟ้ม `MQL5` -> `Files` -> `copytrade` *(ถ้าเข้าไปใน Files แล้วไม่เจอโฟลเดอร์ copytrade ให้คลิกขวาสร้างแฟ้มใหม่ตั้งชื่อว่า copytrade)*
7. ปิดโปรแกรม MT5 แล้วเปิดขึ้นมาใหม่เพื่อให้ระบบรีเฟรชเห็นบอทของเรา!

---

## 📌 บทที่ 2: การเปิด "ศูนย์กระจายสัญญาณ" (Relay Server v3.0)

> [!IMPORTANT]
> - หากคุณต้องการให้ทั้งแม่และลูก ทำงาน **"ในคอมเครื่องเดียวกัน"** ให้ทำขั้นตอนนี้ในคอมพิวเตอร์ของคุณ
> - หากคุณต้องการเปิดรับลูกข่ายจากจังหวัดอื่น/ประเทศอื่น ให้ทำขั้นตอนนี้บน **"VPS"** 

**🔴 สิ่งที่ต้องมี:** ในเครื่องนั้นต้องดาวน์โหลดและติดตั้งโปรแกรม Node.js ไว้แล้ว (เข้าเว็บ `nodejs.org` เลือกปุ่ม LTS และคลิก Next รัวๆ จนติดตั้งเสร็จ)

**การตั้งค่ารหัสผ่าน (.env):**
1. ในโฟลเดอร์ `Server/dashboard` ให้คลิกขวา -> New -> Text Document
2. ตั้งชื่อไฟล์ว่า `.env` (พิมพ์จุดข้างหน้า และต้องไม่มี .txt ต่อท้าย)
3. เปิดไฟล์ด้วย Notepad แล้วก๊อปปี้ข้อความนี้ไปวาง:
   ```env
   AUTH_TOKEN=รหัสผ่านเชื่อมEA
   DASHBOARD_PASSCODE=รหัสเข้าหน้าเว็บ
   HTTP_PORT=8080
   TCP_PORT=5555
   ```
4. กดเซฟ (Ctrl+S) แล้วปิดไฟล์

**วิธีการรัน:**
1. เข้าไปในแฟ้ม `Server` → `dashboard` (ที่คุณได้มาพร้อมกับโปรแกรมตัวนี้)
2. คลิกที่ช่องที่อยู่ Address Bar ด้านบนสุดของหน้าต่างแฟ้ม (ที่เขียนว่า `D:\...\Server\dashboard`) 
3. ลบตัวอักษรทิ้ง พิมพ์คำว่า `cmd` ลงไปแล้วกด Enter -> จะมีหน้าจอสีดำเด้งขึ้นมา
4. พิมพ์: `npm install` แล้วกด Enter *(โหลดส่วนเสริม ทำแค่ครั้งแรก)*
5. พิมพ์: `npm run build` แล้วกด Enter *(รวบรวมไฟล์หน้าเว็บ Dashboard ทำแค่ครั้งแรก หรือเวลามีอัปเดต)*
6. พิมพ์: `npm start` แล้วกด Enter
7. เสร็จสิ้น! ปล่อยหน้าจอดำนี้ทิ้งไว้ตลอดไป **ห้ามกากบาทปิดเด็ดขาด** (นี่คือศูนย์สัญญาณที่กำลังรันอยู่)

> [!NOTE]
> **คำสั่งเทียบเก่า → ใหม่:**
> | เดิม (v2.x) | ใหม่ (v3.0) |
> |---|---|
> | `Server/` → `npm install` → `node server.js` | `Server/dashboard/` → `npm install` → `npm run build` → `npm start` |
> | JavaScript | TypeScript (tsx) |

> [!WARNING]
> **การเจาะช่องทะลุ Firewall (จำเป็นสำหรับ VPS):**
> 1. กดปุ่ม Start พิมพ์คำว่า **Windows Defender Firewall with Advanced Security** แล้วเปิดขึ้นมา
> 2. เมนูด้านซ้ายคลิก **Inbound Rules** -> เมนูด้านขวาคลิก **New Rule...**
> 3. เลือกวงกลมที่คำว่า **Port** -> กด Next
> 4. เลือก TCP และตรงช่อง Specific local ports ให้พิมพ์: `5555, 8080`
> 5. กด Next รัวๆ (เลือก Allow the connection) -> ตั้งชื่อว่า `CopyTrade Server` -> กด Finish

> [!TIP]
> **วิธีเปิดดูแผงควบคุม (Web Dashboard):** 
> หากทำสำเร็จตามด้านบน ให้เปิด Google Chrome ขึ้นมา:
> - ถ้ารันในคอมส่วนตัว: พิมพ์ `http://localhost:8080/`
> - ถ้ารันบน VPS: พิมพ์ `http://<ไอพีของVPSของคุณ>:8080/` 
> คุณจะเจอกับหน้าจอ Dashboard สไตล์ Bento Glassmorphism ที่เอาไว้ดูยอด PnL และสถานะแบบสดๆ!
>
> **✨ ฟีเจอร์ใหม่ระดับ Institutional HUD (v3.0.6):**
> - **Borderless Glassmorphism:** นำเส้นขอบ (Borders) ที่รบกวนสายตาออกทุกจุดเพื่อให้เห็นข้อมูล Telemetry ชัดเจนและดู Premium มากขึ้น
> - **Neural Relationship Monitoring:** ในหน้า Overview ใต้พอร์ต Slave จะเห็นสถานะการติดตาม Master แบบเรียลไทม์
> - **Neural HUD Interface:** หน้าตาโปรแกรมสไตล์ Terminal กึ่งโปร่งใส พร้อมเส้นสแกนและมุมฉากระดับมืออาชีพ
> - **PWA Installation:** ติดตั้ง Dashboard เป็นแอปแยกได้ทันทีผ่าน Chrome
> - **Hybrid Console Layout:** แยกส่วนควบคุมและส่วนจัดการพอร์ตออกจากกันอย่างชัดเจน เพื่อประสิทธิภาพสูงสุด
> - **TradingView Mode:** กราฟพอร์ตขยับทุกๆ 1 วินาที ให้ความรู้สึกเหมือน SITUATION ROOM
> - **Auto-Reconnect:** ระบบเชื่อมต่อใหม่เองภายใน 3 วินาทีหากสัญญาณขาดหาย
>
> **สำหรับนักพัฒนา (Development Mode):**
> เปิด 2 หน้าจอ cmd:
> - หน้าจอที่ 1: `npm run dev:server` (รัน backend + auto-reload เมื่อแก้ไข)
> - หน้าจอที่ 2: `npm run dev` (รัน frontend dev server + hot reload)

---

## ⚠️ บทที่ 3: การตั้งค่า MT5 (บังคับทำก่อนเปิดบอท)

เพื่อให้ MT5 ยอมเชื่อมต่อกับตัวศูนย์กระจายสัญญาณ คุณต้องเปิดทางให้มันก่อน:
1. ใน MT5 กดปุ่ม **Ctrl+O** (หรือไปที่ Tool -> Options)
2. เลือกแท็บ **Expert Advisors**
3. **ติ๊กเครื่องหมายถูกตามนี้:**
   * ✅ Allow algorithmic trading
   * ✅ Allow DLL imports (สำคัญมาก! ถ้าไม่เปิด ระบบจะส่งสัญญาณเครือข่ายไม่ได้)
4. เลื่อนลงมาติ๊ก ✅ Allow WebRequest for listed URL: แล้วกดปุ่มบวก(+) เพิ่มไอพีของเครื่องเซิร์ฟเวอร์เข้าไปในกล่อง (เช่น `http://127.0.0.1` ถ้ารันในเครื่องเดียวกัน)
5. กดปุ่ม OK

---

## 👑 บทที่ 4: สำหรับบัญชีผู้คุม (Master Setup)

1. เปิดกราฟอะไรก็ได้ขึ้นมา 1 กราฟบน MT5 
2. ลากไฟล์ **`CopyTradeMaster`** ด้านซ้ายมือลงมาใส่ในกราฟ
3. จะเจอหน้าต่างตั้งค่า (Inputs):
   * **Relay Host/IP:** 
      * หากศูนย์สัญญาณรันอยู่ในเครื่องนี้เลย: พิมพ์ `127.0.0.1`
      * หากศูนย์สัญญาณรันอยู่ที่ VPS เครื่องอื่น: พิมพ์ IP ของ VPS นั้น (เช่น `128.50.x.x`)
   * **Relay Port:** ใส่ `5555`
4. กด OK สังเกตรูปหมวกสีเขียวขวาบนกราฟเป็นอันเสร็จสิ้น

---

## 📋 บทที่ 5: สำหรับบัญชีผู้ตาม (Slave Setup)

1. เข้า MT5 ลากไฟล์ **`CopyTradeSlave`** ใส่กราฟ
2. จะเจอหน้าต่างตั้งค่า (Inputs) ซึ่งมีตัวเลือกสำคัญที่ต้องอธิบายอย่างละเอียด:

### ⚙️ 5.1 การหาเป้าหมาย (Connections)
* **Relay Host/IP:** กรอกเหมือนบัญชี Master ทุกประการ
* **Master Account ID:** **(สำคัญสุด)** ให้กรอกเลขพอร์ตของบัญชี Master ที่ต้องการลอก (ตัวอย่างเช่น พอร์ตแม่เลข 550999 ก็ให้กรอก 550999 ห้ามผิดเด็ดขาด)

### 💰 5.2 โหมดการออกขนาดออเดอร์ (Lot Settings)
คุณเลือกได้ว่าอยากให้ลูกออกออเดอร์เล็กหรือใหญ่แค่ไหน:
* `LOT_EXACT` : บัญชีแม่ออก 0.1 ฉันจะขอออก 0.1 เป๊ะๆ
* `LOT_RATIO` : ออกเท่านึงของแม่ (เช่น ตัวคูณ=2.0 | แม่ออก 0.1 ฉันจะออก 0.2)
* `LOT_FIXED` : ล็อตตายตัว ไม่ว่าแม่จะออกเท่าไหร่ ฉันจะออกตามล็อตที่ฉันตั้งเอง
* `LOT_BALANCE`: คำนวณเป็นเปอร์เซ็นต์ตามหน้าตักเงินทุนแบบอัตโนมัติ

### ⚖️ 5.3 โหมดสู้ราคา (Execution Mode)
* 👉 **1. EXEC_MARKET — ใช้ราคาตลาดปัจจุบัน:**
  * **ข้อดี:** ออเดอร์ **"ติดแน่นอน 100%"** ไม่มีตกรถ
  * **ข้อเสีย:** ราคาอาจคลาดเคลื่อน (Slippage) เล็กน้อย
* 👉 **2. EXEC_MATCH_MASTER — ใช้ราคา Master + deviation (⭐แนะนำ):**
  * **ข้อดี:** ราคาเข้าใกล้เคียงกับแม่มากที่สุด
  * **ข้อเสีย:** อาจ requote ถ้าราคาวิ่งเร็วมาก
* 👉 **3. EXEC_LIMIT_CHASE — วาง Limit Order ดักราคา:**
  * **ข้อดี:** ราคาเข้าตรงกับบัญชีแม่แบบเป๊ะ
  * **ข้อเสีย:** กราฟมีสิทธิวิ่งหนี ทำให้ "ตกรถ"

---

## 🤖 บทที่ 6: การแก้ปัญหาคู่เงินชื่อแปลกๆ (Symbol Map)

**Q: บัญชี Master ชื่อ `EURUSD` แต่บัญชี Slave เป็นบัญชี Cent ชื่อ `EURUSD.std` ระบบจะลอกได้ไหม?**
**A: ได้แน่นอนครับ 100%!** ระบบมี Fuzzy Deep Scan แค่เห็นว่ามีคำว่า EURUSD มันจะตัดนามสกุลออกแล้วโยงให้เองอัตโนมัติ 

**Q: แล้วถ้าโบรคเกอร์สองที่ ชื่อไม่เหมือนกันเลยล่ะ? (เช่น `XAUUSD` กับ `GOLD`)**
**A: แก้ไขง่ายๆ:**
1. เปิดไฟล์ `SymbolMap.txt` ที่ก๊อปไปวางเมื่อบทที่ 1
2. พิมพ์: `XAUUSD,GOLD` (คั่นด้วยลูกน้ำ ห้ามเว้นวรรค)
3. กดเซฟ (Save) 
4. กลับมาที่หน้าจอบัญชีลูก ตรง Input "ไฟล์ mapping (CSV)" ให้พิมพ์ `SymbolMap.txt`

---

## 📊 บทที่ 7: การใช้งาน Dashboard

Dashboard ใหม่ (v3.0) เป็น **React 19** สไตล์ **Bento Box + Glassmorphism** มี 4 แท็บหลัก:

| แท็บ | เนื้อหา |
|---|---|
| **📊 Overview** | ยอดรวม PnL, Win Rate ภาพรวม, พอร์ต Top Performer, กราฟ Bar Chart สรุป PnL แยกคู่เงิน |
| **📈 Performance** | P&L สรุปชาร์ต, ระบบเตือน Daily Drawdown Guard 5%, SVG ป๊อปอัพพฤติกรรมกราฟ, พับดูข้อมูล Exposure |
| **🔄 Monitor** | Sync Auditor แจ้งเตือนการก๊อปปี้ตกหล่น, บอกสถานะไม้ Routing หรือ Executed ชัดเจน |
| **📋 History** | ประวัติคำสั่งแบบฉบับตารางบัญชี (Ledger), มีระบบ Search/Filter ดูสถิติการได้กำไรสุทธิ Win Rate |

**การเข้าใช้:** เปิดเบราว์เซอร์ไปที่ `http://localhost:8080/` (หรือ URL ของ Cloud deploy)

> [!NOTE]
> Dashboard จะอัปเดตข้อมูลแบบ Real-time ผ่าน WebSocket — ไม่ต้อง refresh หน้า!

---

## 🛡️ บทที่ 8: ความปลอดภัย

| รายการ | คำแนะนำ |
|---|---|
| **AUTH_TOKEN** | ตั้งค่า Token ที่แข็งแรง (อย่างน้อย 16 ตัวอักษร) |
| **DASHBOARD_PASSCODE** | ตั้งรหัสผ่านสำหรับเข้าดู Dashboard |
| **Firewall** | เปิดเฉพาะ Port ที่จำเป็น (5555, 8080) และจำกัด IP |
| **HTTPS** | ใช้ Render.com จะได้ HTTPS อัตโนมัติ — ข้อมูลเข้ารหัสทั้งหมด |

---

## 🧹 บทที่ 9: การทำความสะอาดไฟล์ขยะ (สำหรับผู้ที่อัปเกรดจาก v1/v2 เป็น v3)

เนื่องจากในระบบ CopyTrade v3.0 ได้ปรับโครงสร้างมาเป็น **Clean Architecture** ทำให้ไฟล์เวอร์ชันเก่าหลายๆ ไฟล์หมดความจำเป็น หากท่านอัปเกรดระบบและต้องการล้างไฟล์ขยะที่ไม่ใช้งาน สามารถเปิดโปรแกรม Command Prompt / PowerShell (หรือ Termial ใน VSCode) แล้ว Copy คำสั่งเหล่านี้ไปวางเพื่อลบได้เลยครับ:

**ล้างไฟล์เก่าทั้งหมดในทีเดียว (Windows PowerShell):**
```powershell
Remove-Item -Recurse -Force server.js, modules, src, public, package.json, package-lock.json, start-server.bat, tsconfig.json, tsconfig.node.json, vite.config.ts, tailwind.config.js, postcss.config.js, index.html, auto-commit.js -ErrorAction SilentlyContinue
```

> [!TIP]
> สำหรับผู้ใช้งานบนเซิร์ฟเวอร์ Linux (Ubuntu/Debian) ใช้คำสั่งนี้:
> ```bash
> rm -rf server.js modules src public package.json package-lock.json start-server.bat tsconfig.json tsconfig.node.json vite.config.ts tailwind.config.js postcss.config.js index.html auto-commit.js
> ```

---

*CopyTrade Pro v3.0 — Clean Architecture, TypeScript, Institutional-Grade Execution*
